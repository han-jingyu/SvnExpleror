<li class="{op:} parity{parity:}">
    <span data-op="{op:}" class="prop-add">添加属性：{name:escape}</span>
    <span data-op="{op:}" class="prop-delete">删除属性：{name:escape}</span>
    <span data-op="{op:}" class="prop-modify">修改属性：{name:escape}</span>
    {lines-list:}
</li>